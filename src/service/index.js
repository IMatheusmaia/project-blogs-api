const userService = require('./user.services');
const categoryService = require('./category.services');
const postService = require('./post.services');
const postCategoryService = require('./postCategory.services');

module.exports = {
  userService,
  categoryService,
  postService,
  postCategoryService,
};